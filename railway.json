{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python -c \"import os; print('🔍 DATABASE_URL:', os.getenv('DATABASE_URL', 'NÃO DEFINIDA')[:50]); print('🔍 Todas as vars:', [k for k in os.environ.keys() if 'postgres' in k.lower() or 'database' in k.lower() or 'railway' in k.lower()]); exit(1 if 'localhost' in os.getenv('DATABASE_URL', '') else 0)\" || echo '❌ PostgreSQL não configurado - adicione PostgreSQL ao projeto Railway'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}

